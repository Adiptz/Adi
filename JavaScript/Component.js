import React from

react;

const Rewire = ({buttonText, logo, backgroundColor, cardWrap}) => {

    if (cardWrap) {
        cardStyle = {
            width
        }
    }
}
let logo = logo ? <img src='logo'>: null;
    return (
    <div style={{color: backgroundColor}}>
        <button>{buttonText ? buttonText : ''}</button>
        {logo}
        <div style>
            <p>{Children}</p>
        </div>
    </div>
    );

    }