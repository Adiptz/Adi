import React from "react";
import CheckBoxMenu from "../Components/CheckboxMenu/CheckBoxMenu";

const Custom = ({classSizes, annualWages}) => (
        <CheckBoxMenu classSizes={classSizes} annualWages={annualWages}/>
)

export default Custom;